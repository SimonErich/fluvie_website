# fluvie.dev

The Fluvie marketing site, served at https://fluvie.dev via GitHub Pages
(deploy from `main`).

The page is a single static `index.html` plus the logos and `og.png`. Its source
lives in the Fluvie monorepo under `web/landing/`; update it there, then copy the
page here.
